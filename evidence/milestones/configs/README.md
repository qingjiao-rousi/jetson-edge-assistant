# Frozen Milestone Configurations

`vlm/` contains immutable configuration evidence for completed M7/M8 VLM milestones.
These files document the conditions used for frozen results and are not current
application runtime configuration. Current application configuration remains in
the repository-root `configs/` directory.
