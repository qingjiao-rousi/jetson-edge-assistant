# Historical RAG Configurations

These configurations support reproduction and audit of historical RAG experiments
only. Current application execution uses `configs/manual-retrieval.json` and
`configs/embedding.json`; do not use this directory as a runtime default.
