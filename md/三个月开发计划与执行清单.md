# Jetson 端侧离线多模态推理系统：三个月开发计划与执行清单

项目背景：[项目背景](./项目背景.md)

项目概述：[项目实践：Jetson 端侧离线多模态推理与设备故障辅助系统](./项目实践-jetson端侧离线多模态智能助手.md)

技术设计：[任务需求与技术设计说明](./项目实践-jetson端侧任务需求.md)

> 文档用途：将需求文档转换为每周可执行任务、交付物和验收门。
>
> 个人主线：Jetson Runtime、LLM/VLM、量化性能、RAG/工具和部署演示。
>
> 团队边界：实时语音属于项目最终能力，但不计入个人必须实现范围；个人交付稳定接口和契约测试，客户演示及最终音视频联调由其他成员推进。
>
> 项目结束状态：完成可集成 Runtime 模块、问题记录和模块技术交接；客户演示不作为个人交付项，实习结束时不表述为通过最终验收或正式生产交付。

## 1. 开发目标

在 Jetson AGX Orin 32GB 上，基于 `llama.cpp-omni` 二次开发一个离线 LLM/VLM Runtime 和设备故障辅助原型，形成以下闭环：

~~~text
GGUF 模型与版本校验
        ↓
Jetson CUDA Runtime 与自有 C++ Adapter
        ↓
文本/图片推理、流式输出、请求生命周期
        ↓
量化、KV Cache、Prefill/Decode 与资源评测
        ↓
本地 RAG、受限工具和故障报告
        ↓
Docker/systemd 离线部署与模块技术交接
~~~

## 2. 当前基线

### 2.1 已完成

- [x] Jetson AGX Orin、L4T、CUDA、GCC、CMake 环境审计；
- [x] `llama.cpp-omni` Release + GGML_CUDA 构建；
- [x] 定位并修复 mtmd include 的 CMake PRIVATE/PUBLIC 传递问题；
- [x] Qwen2.5-3B-Instruct Q4_K_M GGUF metadata 校验；
- [x] tokenizer 和 chat template 校验；
- [x] 模型本地 SHA-256 记录；
- [x] 中文文本推理冒烟；
- [x] CUDA0: Orin 初始化；
- [x] 37/37 可 offload 层全部分配到 GPU；
- [x] CUDA model/KV/compute buffer 记录；
- [x] tegrastats 的 GPU、RAM、温度和功耗采集；
- [x] 单次 Prefill/Decode 数据记录。
- [x] 获取带 Git 元数据的个人 fork，固定上游基线 `74699a5`；
- [x] 将 CMake 修复提交并推送为 `jetson-runtime-dev@19cc269`；
- [x] 在 fork 目录重新完成 Release + GGML_CUDA 构建并生成 `llama-cli`。
- [x] 建立 environment/build/runtime/model 四份 manifest；
- [x] 固定 SM 87、关闭单 GPU 不需要的 NCCL，并完成产物 hash 链验证；
- [x] 完成 1 次独立 preconditioning + 5 次有效 benchmark；
- [x] 保存 JSONL/CSV、原始 stdout/stderr、tegrastats 和可提交的统计摘要；
- [x] 创建主项目基线 commit `9b63aad`，benchmark 记录 clean commit。

### 2.2 第1周遗留项

- [ ] 尚未建立统一的一键 Runtime 构建脚本；
- [ ] 常驻 DirectBackend 尚未实现；CLI 结果不能替代模型单例、TTFT/TPOT、Cancel 或多请求生命周期验证；
- [ ] Q8_0、KV Cache、VLM、RAG 和部署属于后续阶段，尚未开始。

## 3. 三个月主线

| 时间 | 个人主线 | 阶段交付物 |
| --- | --- | --- |
| 第1周 | Jetson 基线、模型选型、CUDA 构建 | 可追溯源码、模型 manifest、重复 benchmark |
| 第2～4周 | C++ Runtime、文本服务、流式输出 | EdgeOmni Runtime、请求生命周期、文本服务 |
| 第5～6周 | 量化、KV Cache、Prefill/Decode 和性能 | Q4/Q8、KV Cache 与资源对比报告 |
| 第7～8周 | VLM 图片、视觉指标 | 图片推理、vision/image token 指标 |
| 第9～10周 | RAG、工具、Agent、多 session | 设备故障辅助业务闭环 |
| 第11周 | Docker/systemd、Jetson 演示环境 | ARM64 演示包、服务启动和日志 |
| 第12周 | 模块验收、问题记录和技术交接 | 接口契约测试、问题清单、模块交接文档 |

## 4. 第1周：基线、模型选型和可复现构建

### 4.1 目标

把“一次运行成功”固化成任意团队成员可以重复的 Runtime 和模型基线。

### 4.2 任务

1. 获取带 Git 元数据的 `llama.cpp-omni` 个人 fork；
2. 记录 fork URL、branch、commit 和上游基线；
3. 比较当前源码快照与 fork，排除未记录差异；
4. 将 CMake include 修复迁移为独立 commit 或 patch；
5. 使用 out-of-source 目录重新配置和构建；
6. 编写统一环境/构建检查脚本；
7. 为每个候选模型建立 manifest；
8. 限定 2～3 个 LLM，使用统一 Prompt、context 和量化等级测试；
9. 固定主模型；
10. 实现独立 preconditioning + 至少 5 次重复 benchmark；
11. 输出 JSONL/CSV 和 Markdown 汇总；
12. 保存 tegrastats 原始数据和运行配置。

### 4.3 模型选型范围

最多测试：

- Qwen2.5-3B-Instruct Q4_K_M；
- Qwen3-4B Q4_K_M；
- 一个 3B～4B 备选模型。

只对最终主模型继续进行 Q8_0、高精度和 KV Cache 矩阵测试。

### 4.4 交付物

~~~text
third_party/llama.cpp-omni 或明确 fork 引用
patches/
configs/baseline/
scripts/build/
scripts/benchmark/
models/manifests/
docs/baselines/
~~~

### 4.5 验收门

- Runtime commit 可查询；
- 干净环境能按脚本完成配置和构建；
- 模型来源、revision、大小、SHA-256 和 GGUF metadata 可查询；
- 同一配置至少 5 次重复；
- Prompt/Generation throughput、TTFT/TPOT 或可获得的原始 timing 有统计；
- tegrastats 与每次运行有时间对应关系；
- 未实测数据不进入结论。

## 5. 第2～4周：C++ Runtime 与文本服务

### 5.1 目标

形成自有代码主体，使上层业务不直接依赖 `llama.cpp-omni` 内部对象或命令行格式。

### 5.2 第2周：接口和最小文本闭环

- 定义 `RuntimeConfig`、`GenerateRequest`、`GenerateResponse`；
- 定义 `RuntimeStatus` 和错误码；
- 定义 `EdgeOmniRuntime` 抽象接口；
- 以 `DirectBackend` 为核心实现，直接链接 `libllama`；
- `llama-cli` 只保留为基线和故障对照，不进入业务调用链；
- 配置化模型、context、GPU layers 和 sampling；
- 接入 `llama_log_set` 日志回调并映射模型加载、Tokenize、Decode 和取消状态；
- 使用 fake backend 完成单元测试；
- 真实模型测试标记为 Jetson integration。

验收：`DirectBackend` 能在同一进程加载 Qwen2.5 并返回结构化结果，FakeBackend 单元测试不依赖 GPU。

### 5.3 第3周：模型生命周期和流式输出

- Runtime 初始化与 shutdown；
- 模型只加载一次；
- 流式 token callback/SSE；
- request ID 和日志关联；
- model info；
- runtime metrics；
- 上下文重置；
- 原始日志保存。

验收：连续请求不重复加载模型，流式首 token 可见，模型和请求可追踪。

### 5.4 第4周：超时、取消和错误处理

- 请求 timeout；
- cancel；
- Runtime 异常和服务进程退出处理；
- context 超限；
- 模型不存在/hash 不符；
- CUDA/OOM；
- 正常停止；
- HTTP/JSON 服务原型；
- 如确有需要，记录 Ollama-compatible 字段范围。

验收：故障注入不会导致静默退出，错误均有稳定错误码和日志。

## 6. 第5～6周：量化、KV Cache 和性能

### 6.1 第5周：权重量化

只针对固定主模型比较：

- Q4_K_M；
- Q8_0；
- F16/BF16 高精度参考，前提是实际可获得且可加载。

固定：

- 模型 revision；
- tokenizer/chat template；
- Prompt 和目标输出；
- context、batch、ubatch；
- GPU layers；
- Flash Attention；
- nvpmodel 和散热条件；
- warmup 与重复次数。

输出：模型大小、加载时间、TTFT、TPOT、吞吐、RAM、温度、功耗和固定任务质量。

### 6.2 第6周：KV Cache 和长上下文

- F16 与 Runtime 实际支持的 Q8_0 等 KV 类型；
- 短、中、长 context；
- KV Cache 大小；
- Prefill/Decode；
- OOM 边界；
- 输出稳定性；
- Prompt cache 的启用/禁用条件。

验收：每项结论都有配置、原始数据和失败记录，不把上游工具能力表述为自研算法。

## 7. 第7～8周：VLM 图片与视觉指标

### 7.1 第7周：VLM 基线

- 选择一个实际支持的 VLM；
- 记录主模型、vision/mmproj 和其他模块的来源与 hash；
- 跑通单张静态图片；
- 支持设备照片和告警截图；
- 处理格式、大小、分辨率和损坏图片错误；
- 不进入实时视频。

### 7.2 第8周：统一接口和视觉性能

- `generate_with_image()`；
- 文本/图片统一 Request；
- image token；
- vision preprocess/encode time；
- TTFT、TPOT 和 total latency；
- 固定图片评测集；
- VLM RAM、温度和功耗；
- 流式输出。

验收：固定设备图片集能稳定输出，视觉耗时和资源有结构化记录。

### 7.3 实时语音团队边界

实时语音是项目最终能力，可以由其他员工负责：

~~~text
麦克风 → VAD/AEC → ASR → EdgeOmni Runtime → TTS → 播放
~~~

个人交付限定为 Runtime 接口设计及 Mock/契约测试，包括结构化文本输入、流式 Token 输出、Cancel 和 Context Reset。ASR、TTS、播放、全双工流控、最终联调及演示均由相应负责成员推进，不作为个人模块的验收条件。

## 8. 第9～10周：RAG、工具、Agent 和多 session

### 8.1 第9周：本地 RAG

- PDF、Markdown、纯文本和日志；
- 文档解析和切分；
- 本地 embedding；
- 本地向量索引；
- Top-K；
- 上下文拼接；
- 来源文档和片段 ID；
- 固定问答集。

验收：回答能返回真实来源；检索失败不伪造引用；正式运行不依赖云端 embedding。

### 8.2 第10周：受限工具和多 session

工具限定为 2～3 个：

- 文件搜索/读取；
- JSON/故障码查询；
- 日志统计或报告生成。

同时实现：

- input/output Schema；
- 权限根目录；
- timeout 和错误码；
- 工具审计日志；
- 基础 session 隔离；
- context reset；
- 请求排队和资源上限。

Agent 只做受控编排：判断是否检索、选择工具、生成带引用报告，不实现无限循环通用 Agent。

## 9. 第11周：Docker/systemd 与演示环境

### 9.1 systemd

- 非 root 用户；
- 配置和模型路径；
- 启动前模型检查；
- 健康检查；
- 正常停止；
- 异常重启；
- 日志和轮转。

### 9.2 ARM64 Docker

- Jetson 兼容镜像；
- Runtime 和应用打包；
- 模型只读挂载；
- GPU/设备节点映射；
- 离线镜像导入；
- image digest；
- 宿主机与容器 CUDA 检查。

验收：至少一个部署方式可以完整演示；另一个如未完成，明确记录遗留问题。

## 10. 第12周：模块验收和技术交接

### 10.1 目标

交付可被上层继续集成的 Runtime 模块和完整交接材料，不承担客户演示或最终生产验收。

### 10.2 任务

- 固定验收模型、配置和样例；
- 准备文本、图片和 RAG/工具模块验收流程；
- 部署 Docker/systemd 模块验证环境；
- 执行请求、流式输出、Cancel 和 Context 的接口契约测试；
- 记录模块测试问题和集成注意事项；
- 整理已知限制；
- 整理构建、模型、部署和排障文档；
- 输出未完成项和后续建议；
- 完成源码、模型 manifest、测试数据和文档交接。

### 10.3 状态表述

允许：

> 完成可集成 Runtime 模块、Docker/systemd 部署验证和阶段性技术交接。

不允许：

- 已通过最终客户验收；
- 已正式生产上线；
- 已完成生产级长稳；
- 已完成全部异常恢复和模型回滚。

## 11. 推荐代码结构

~~~text
vlmllm-main/
  runtime/
    include/
    src/
    CMakeLists.txt
  app/
    backend/
    request/
    response/
    service/
    metrics/
    rag/
    tools/
  third_party/
    llama.cpp-omni/
  patches/
  configs/
    baseline/
    models/
    service/
  models/
    manifests/
  scripts/
    build/
    benchmark/
    deploy/
  tests/
    unit/
    integration/
    benchmark/
  docker/
  systemd/
  docs/
    baselines/
    benchmarks/
    decisions/
    handover/
~~~

## 12. Definition of Done

一个任务只有同时满足以下条件才标记完成：

- 有代码、配置或脚本；
- 有可重复运行命令；
- 有成功和失败测试；
- 有原始日志或结构化结果；
- 有版本、模型和环境信息；
- 已知限制已记录；
- 不依赖个人未记录的手工步骤；
- 能解释上游能力和个人修改边界。

## 13. 状态管理

每项任务使用：

- `TODO`：未开始；
- `DOING`：进行中；
- `BLOCKED`：有明确阻塞；
- `DONE`：满足 Definition of Done；
- `DEFERRED`：移出当前周期。

每周结束记录：

~~~text
本周完成
真实测试结果
失败和风险
未完成原因
下周任务
需要团队协作的接口
~~~

## 14. 当前下一步

第 1 周已完成，当前进入第 2 周。按顺序执行：

1. 提交第 1 周收口的选型、盲评、启动器和周报变更；
2. 审计固定 Runtime 的 DirectBackend 实际 C/C++ API、链接 target 和资源所有权；
3. 冻结 `RuntimeConfig`、`GenerateRequest`、`GenerateResponse`、`RuntimeStatus` 和 `RuntimeMetrics` 接口；
4. 接口确认后创建 `runtime/` CMake 工程、FakeBackend 单元测试和最小 DirectBackend；
5. 仅实现 Qwen3 文本模型单例加载、单次生成、连续请求、Context Reset 和 shutdown；
6. 最小闭环通过 Jetson 集成测试后，再进入流式 callback、Cancel、HTTP/SSE 和真实 TTFT/TPOT 采集。
